data Inside:
    foo: i32
    bar: bool

data Wrapping:
    inside: Inside
    value: string

data Enclosing:
    wrap: Wrapping
    arr: []Wrapping
    flag: bool

func call(enc: Enclosing, oni32: i32 -> (), onString: string -> (), onBool: bool -> (), onInside: Inside -> Inside) -> Inside:
    onBool(enc.flag)
    onBool(enc.wrap)


