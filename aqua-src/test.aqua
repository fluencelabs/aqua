service OpH("op"):
  get_str(s: string) -> string
  pr() -> []string
  op()
  get_arr() -> []string
  identity: ⊤ -> ⊥

func registerKeyPutValue(nodes: []string):
  for n <- nodes:
    on n:
      OpH.op()
