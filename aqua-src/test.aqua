service OpH("op"):
  get_str() -> string
  pr() -> []string
  op(s: string)
  get_arr() -> []string

func registerKeyPutValue(node_id: string) -> []string:
  on node_id:
    nodes <- OpH.pr()
  on node_id:
      for n <- nodes par:
        on n:
          OpH.op("in")
  <- nodes