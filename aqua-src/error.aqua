import "demo.aqua"

func one() -> u64:
    variable <- Demo.get4()
    <- variable

func two() -> u64:
    variable <- one()
    <- variable

func str(xs: []u64, z: u64):
    $stream: bool
    for x <- xs:
       $stream <- Demo.get4()
    $stream <- z


file1
const x = "privet"

func foo(y: string): ....

file2
func bar():
   foo(x) -> telo foo -> y ::::: x -> x :::: "privet"