service A("a"):
  use(arr: []string)

func use_stream(stream: *string) -> *string:
  stream <<- "value"
  <- stream

func test():
  arr = [1,2,3,4,5]
  for a <- arr:
    r <- use_stream(*[])
    A.use(r)

