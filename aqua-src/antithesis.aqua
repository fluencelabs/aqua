aqua StreamMapTest declares *

export testForTupleFunc

import "builtin.aqua"

func testForTupleFunc() -> []string, []string:
  streamMap: %string
  streamMap <<- ("key one", "1")
  streamMap <<- ("key one", "2")

  streamMap <<- ("key four", "7")

  streamFirst: *string
  streamSecond: *string

  for k, v <- streamMap:
    streamFirst <<- k
    streamSecond <<- v

  for k, v <- streamMap:
    streamFirst <<- v
    streamSecond <<- k


  <- streamFirst, streamSecond