aqua Test

export test

service Srv("srv"):
    c(n: i32)

func useArr(arr: []i32) -> i32:
    b = arr[0]
    <- b

func test() -> i32:
    stream: *i32
    stream <<- 42
    res <- useArr(stream)
    <- res