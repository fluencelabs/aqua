aqua Job declares *

export testContainsFunc

func testContainsFunc() -> bool, bool, bool, bool, bool:
  streamMap: %string
  resFirst = streamMap.contains("key one")
  streamMap <<- ("key one", "")
  resSecond = streamMap.contains("key one")
  resThird = streamMap.contains("key one")
  streamMap <<- ("key one", "new")
  streamMap <<- ("key two", "")
  resFourth = streamMap.contains("key one")
  resFifth = streamMap.contains("key two")
  <- resFirst, resSecond, resThird, resFourth, resFifth
