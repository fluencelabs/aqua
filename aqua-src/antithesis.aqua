service Console("run-console"):
    print(s: string)

func main():
    ss: *string
    dd: *string
    peerId = "peerId"
    relay = "relay"
    parsec s <- ss on peerId via relay:
      Console.print(s)
    for d <- dd par:
      Console.print(d)
