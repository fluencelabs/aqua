aqua StreamMapTest declares *

export testForTupleFunc


func testForTupleFunc() -> []string:
  streamMap: %string
  streamMap <<- ("key one", "1")

  streamThird: *string

  for k, v <- streamMap:
    streamThird <<- streamMap.get(k)!

  <- streamThird