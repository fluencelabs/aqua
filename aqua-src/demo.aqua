import "builtin.aqua"

service OpH("op"):
  puk(s: string) -> string

func a(b: string) -> string:

  try:
    f = "world"
    OpH "planet"
    OpH.puk("TRY THIS")
  catch err:
    OpH.puk(err.msg)
  <- f

