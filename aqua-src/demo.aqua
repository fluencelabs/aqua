import "builtin.aqua"

service OpH("op"):
  puk(s: string) -> string
  pek(s: string, c: string) -> string

func a(b: string,
  c: string,
  g: string) -> string:

  try:
    f = "world"
    OpH "planet"
    OpH.pek("TRY THIS",
            c)
  catch err:
    OpH.puk(err.msg)
  <- f

